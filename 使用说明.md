# 脚本分镜工具 · 使用说明

双击 `index.html` 用浏览器打开即可用（推荐 Chrome / Edge）。**不联网也能用，脚本内容不会上传到任何地方。**

## 六个功能

| 页签 | 干什么 | 要点 |
|---|---|---|
| ① 加分镜 | 给没有标注的脚本补【分镜】 | 两种模式：**真人出镜**（口播行标【出境】）/ **无出镜**（口播行只标实拍镜头）；未命中的行标黄留空，人工补 |
| ② 去出镜 | 把【出境】换成实拍镜头 | 永远不会产出禁用标签（画外音/黑底白字等，可在规则里配） |
| ③ 加挂载 | 按日期给脚本加【挂私信】【挂定位】 | 规则：当天1条→私信；2条→私信/定位；3条→私信/定位/私信；写在脚本第一行 |
| ④ 去分镜 | 删掉所有分镜【…】 | 白名单（bgm/卡点/门牌号/展示要求）会保留；词库外的【…】保留并提示确认 |
| ⑤ 替换表 | 城市/面积/区域/标志建筑 双向替换 | `→ 填入实值`（xx（城市）→厦门）、`← 还原占位符`（厦门→xx（城市））；「佛山工厂」这类保护词不受影响 |
| ⑥ 红线检测 | 扫极限词/自定义词，**只标红提示，不改字** | 分组开关（厦门开极限词组、佛山开产地词组）；可导出检测报告 |

## 用法

1. 顶部选**门店**（厦门画巢 / 佛山画巢 / 通用）——规则整套跟着换
2. 进对应页签 → **拖 Excel 进去**（或点一下选文件）
3. 点**开始处理** → 看预览（黄色=待人工补、红色=红线词）
4. 点**导出 Excel**，得到处理后的表

## 更新规则（不用重新发文件）

- 规则都在 `rules.js` 里：**改这个文件 → 刷新页面就生效**
- 页面上「⚙ 规则库」可以：
 - 直接改 JSON → 保存到本机（只影响你这台电脑）
 - 导出规则文件 → 发给同事，对方点「导入规则文件」
 - 恢复默认 → 回到 rules.js 里的版本
- 部署到 GitHub Pages 后，**改 rules.js 推送一次，所有人刷新即最新，链接不变**

## 目录结构

```
分镜工具/
├── index.html   界面
├── app.js       处理逻辑
├── rules.js     规则包（加分镜规则/替换表/红线词库/白名单/挂载规则）
├── rules.json   规则包（同内容，便于查看/编辑）
└── lib/xlsx.full.min.js   Excel 读写库（离线内置）
```

## 规则怎么调（常见）

| 想改什么 | 改 rules.js 里的 |
|---|---|
| 某个词该标什么镜头 | `add_rule.rules` 里加一条 `{"name":"...","pat":"关键词正则","tag":"标签"}` |
| 产品/款式名单（影响【单品*N】计数） | `add_rule.products` |
| 风格名单（影响【风格*N】） | `add_rule.style_words` |
| 城市/面积/区域/标志建筑的替换 | `replace_table`（`vals` 可放多个实际值） |
| 不被替换的词（如佛山工厂） | `replace_table_protect` |
| 红线词 | `redline_groups`（分组、可开关） |
| 去分镜时不删的【…】 | `whitelist`（exact / contains / regex） |
| 挂载规则 | `guazai_rule.by_count` |
| 去出镜能用的镜头、禁用镜头 | `remove_chujing.candidates` / `banned_output` |
